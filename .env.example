# ========================================
# ZoneSport - Variables de Entorno
# ========================================
# Este archivo (.env.example) es DOCUMENTACIÓN de las variables necesarias
# Para desarrollo: copia a server/.env y reemplaza los valores
# SEGURIDAD: El archivo .env real NUNCA debe ser commiteado

# ========================================
# DATABASE CONFIGURATION (TypeORM)
# ========================================
# Local development
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=zonesport_user
DB_PASSWORD=your_very_secure_password_minimum_16_chars
DB_NAME=zonesport_db
DB_LOGGING=false

# ========================================
# AUTHENTICATION (JWT)
# ========================================
# Generar secretos seguros: openssl rand -base64 32
JWT_SECRET=your_very_long_random_jwt_secret_minimum_32_characters_recommended
JWT_RESET_SECRET=your_very_long_random_reset_secret_minimum_32_characters_recommended
JWT_EXPIRATION=24h

# ========================================
# SERVER CONFIGURATION
# ========================================
NODE_ENV=development
PORT=3001

# ========================================
# FRONTEND INTEGRATION (CORS & Password Reset Links)
# ========================================
FRONTEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# ========================================
# EMAIL SERVICE (Optional - Resend)
# ========================================
# Get API key from: https://resend.com
# Leave empty to disable email functionality
RESEND_API_KEY=re_your_api_key_from_resend
RESEND_FROM_EMAIL=noreply@yourdomain.com
RESEND_DOMAIN=yourdomain.com

# ========================================
# FRONTEND NEXT.JS PUBLIC VARIABLES
# ========================================
# ⚠️ IMPORTANTE: NEXT_PUBLIC_ variables son visibles en el navegador
# NUNCA incluya secretos en variables NEXT_PUBLIC_
NEXT_PUBLIC_API_URL=http://localhost:3001

# ========================================
# PRODUCCIÓN - Render + Vercel
# ========================================
# Cuando despliegues a producción, actualiza estos valores:
#
# FRONTEND_URL → Tu URL de Vercel (ej: https://zonesport.vercel.app)
# CORS_ORIGIN → Tu URL de Vercel (ej: https://zonesport.vercel.app)
# NEXT_PUBLIC_API_URL → Tu URL de backend en Render (ej: https://zonesport-api.render.com)
#
# DB_HOST → Host de PostgreSQL en Render
# DB_USERNAME → Usuario de Render con permisos de BD
# DB_PASSWORD → Contraseña fuerte (mínimo 16 caracteres)
# DB_NAME → Nombre de la base de datos en Render
#
# JWT_SECRET → Generar nuevo: openssl rand -base64 32
# JWT_RESET_SECRET → Generar nuevo: openssl rand -base64 32
#
# RESEND_API_KEY → Tu API key de Resend (NO la de desarrollo)
# RESEND_FROM_EMAIL → Tu dominio en producción
# RESEND_DOMAIN → Tu dominio de email en producción
