# Database Configuration
POSTGRES_USER=ExampleUser
POSTGRES_PASSWORD=ExamplePassword
POSTGRES_DB=zonesport_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# JWT Configuration
JWT_SECRET=your-super-secret-key-change-this-in-production
JWT_RESET_SECRET=your-reset-token-secret-key-change-this-in-production

# Email Configuration (SMTP)
# Option 1: Gmail (requires app-specific password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
SMTP_FROM=noreply@zonesport.com

# Option 2: Mailtrap (for development)
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_SECURE=false
# SMTP_USER=your-mailtrap-username
# SMTP_PASSWORD=your-mailtrap-password

# Frontend URL (for reset password links)
FRONTEND_URL=http://localhost:3000

# Environment
NODE_ENV=development
